<?xml version="1.0"?>
<odoo>
    <!-- Action window pour afficher la liste des propriétés -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="context">{'search_default_date_availability': True}</field>
        <field name="res_model">estate_property</field>
        <field name="view_mode">list,form</field>
    </record>  
    

    <!-- Vue liste pour le modèle estate.property -->
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <list string="Properties List" decoration-success="state in ('offer_received', 'offer_accepted')" decoration-muted="state == 'sold'" decoration-bf="state == 'offer_accepted'">
                <field name="name"/>
                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="property_type_id"/>
                <field name="state" invisible="True"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="date_availability" optional="True"/>
            </list>
        </field>
    </record>

    <!-- Vue form pour le modèle estate.property -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <form string="Test">
                <header>
                    <button name="action_sold_property" type="object" string="Sold" invisible="state in ['sold','cancel']"/>
                    <button name="action_cancel_property" type="object" string="Cancel" invisible="state in ['sold','cancel']"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold,canceled"/>
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <newline/>
                        <group>
                            <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            <field name="property_type_id"  options="{'no_create': True, 'no_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/>
                        </group>                        
                    </group>
                    <notebook>                       
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <field name="facades"/>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_area" invisible="not garden"/>
                                    <field name="garden_orientation" invisible="not garden"/>
                                    <field name="total_area"/>
                                    <field name="state"/>
                                </group>  
                            </group>                                               
                        </page>
                        <page string="Other Info">
                            <group>
                                <group>
                                    <field name="res_partner_id"/>
                                    <field name="res_users_id"/>
                                </group>  
                            </group>                                               
                        </page>
                        <page string="Offer">
                            <field name="offer_ids" readonly="state in ['offer_accepted','sold','canceled']" editable="bottom"/>                                          
                        </page>                        
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

     <!-- Vue Search pour le modèle estate.property -->
    <record id="estate_property_view_search" model="ir.ui.view">
            <field name="name">estate.property.search</field>
            <field name="model">estate_property</field>
            <field name="arch" type="xml">
                <search string="Estate Property">
                    <field name="name"/>
                    <field name="property_type_id"/>
                    <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                    <filter string="Available" name="date_availability" domain="[('date_availability', '>', context_today()),'|',('state', '=', 'new'),('state', '=', 'offer_received')]"/>
                    <group expand="1" string="Group By">
                        <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                    </group>
                </search>
            </field>
        </record>
</odoo>